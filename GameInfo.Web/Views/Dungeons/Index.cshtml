@model List<GameInfo.Models.ViewModels.DungeonsAllViewModel>
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<br />
<div class="container-fluid" style="border: solid 1px white; border-radius: 10px;">
    @if (Model.Count == 0)
    {
        <h4 class="text-white text-center" style="padding-top: 4px">No dungeons have been added yet.</h4>
        <hr style="background-color: white" />
    }
    else
    {
        <div class="row text-white" style="padding: 0px 12px 0px 12px; border-bottom: 1px solid white">
            <div class="col-8 text-left" style="border-right: 1px solid white; padding-top: 1%">
                <p><b>Name</b></p>
            </div>
            <div class="col-4 text-left" style="padding-top: 1%">
                <p><b>Boss count</b></p>
            </div>
        </div>
        @foreach (var dungeon in Model.OrderBy(x => x.Name))
        {
            <div class="row text-white" style="padding: 0px 12px 0px 12px; border-bottom: 1px solid gray">
                <div class="col-8 text-left" style="border-right: 1px solid white; padding-top: 1%">
                    <p>
                        <a asp-controller="Dungeons" asp-action="Details" asp-route-id="@dungeon.Id" style="color:white">
                            <i>@dungeon.Name</i>
                        </a>
                    </p>
                </div>
                <div class="col-4 text-left" style="padding-top: 1%">
                    <p>
                        <i>@dungeon.BossCount</i>
                    </p>
                </div>
            </div>
        }
    }
    <div class="row" style="padding-top: 2px">
        <div class="col-6">
            <form asp-controller="Dungeons" asp-action="Add" method="get">
                <button type="submit" class="btn" style="margin: 4px">Add a dungeon</button>
            </form>
        </div>
        <div class="col-6 text-right">
            <form asp-controller="Home" asp-action="Index" method="get">
                <button type="submit" class="btn" style="margin: 4px">Back to Index</button>
            </form>
        </div>
    </div>
</div>
<br />